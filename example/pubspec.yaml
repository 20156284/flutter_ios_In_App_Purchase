name: flutter_ios_in_app_purchase_example
description: Demonstrates how to use the flutter_ios_in_app_purchase plugin.
publish_to: 'none'

environment:
  sdk: ">=2.0.0-dev.28.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_ios_in_app_purchase:
    path: ../

flutter:

  uses-material-design: true
